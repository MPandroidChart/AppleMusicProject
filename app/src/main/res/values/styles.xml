<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/mianColor</item>
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
        //statusBarColor优先级更高；
    </style>

    <style name="AnimationActivity" parent="android:style/Animation.Activity">
        <!--打开Activity，新进入的Activity执行的动画-->
        <item name="android:activityOpenEnterAnimation">@anim/open_enter</item>
        <!--打开Activity，退出的Activity执行的动画-->
        <item name="android:activityOpenExitAnimation">@anim/open_exit</item>
        <!--关闭Activity，新进入的Activity执行的动画-->
        <item name="android:activityCloseEnterAnimation">@anim/close_enter</item>
        <!--关闭Activity，退出的Activity执行的动画-->
        <item name="android:activityCloseExitAnimation">@anim/close_exit</item>

    </style>
    <!--分割线-->
    <style name="line">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/lineColor</item>
        <item name="android:layout_margin">10dp</item>
    </style>
    <!--提交按钮-->
    <style name="commitButton">
        <item name="android:layout_height">44dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:textSize">18sp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/bt_commit_select</item>
    </style>
</resources>
